# Cloudflare Workers local development environment variables
# These are only used locally with `wrangler dev`
# For production, set these in Cloudflare Dashboard > Workers & Pages > Settings > Variables

SESSION_SECRET=dev_only_session_secret_at_least_32_characters_long_for_local
