{
  "name": "contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Datacenter Builder SBT",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:amoy": "hardhat run scripts/deploy.js --network polygon-amoy",
    "deploy:polygon": "hardhat run scripts/deploy.js --network polygon",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:bsc-testnet": "hardhat run scripts/deploy.js --network bsc-testnet",
    "test": "hardhat test",
    "deploy:ksn:sepolia": "hardhat run scripts/deploy-ksn.js --network sepolia",
    "local:demo": "hardhat run scripts/local-demo.js --network localhost",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  }
}
